import React from 'react';

// Styled components
import { Item, Circle } from './Skeleton.styles';

const Skeleton = () => {
  return (
    <Item>
      <Circle />
    </Item>
  );
};

export default Skeleton;
